import React from 'react';

import { StyledMenu } from './styles';

const Menu = () => {
  return(
    <StyledMenu>
      <p>I'm a styled menu</p>
    </StyledMenu>
  );
};

export default Menu;
